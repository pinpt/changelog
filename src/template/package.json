{
  "name": "theme",
  "description": "My Theme",
  "version": "1.0.0",
  "license": "MIT",
  "changelog": {
    "version": "2",
    "darkMode": true
  },
  "scripts": {
    "build": "npx @pinpt/changelog build --site SITE",
    "dev": "npx @pinpt/changelog dev --site SITE",
    "package": "npx @pinpt/changelog package --site SITE"
  },
  "dependencies": {
    "@pinpt/changelog": "^2.0.0"
  }
}
