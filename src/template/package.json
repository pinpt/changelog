{
  "name": "theme",
  "description": "My Theme",
  "version": "1.0.0",
  "license": "MIT",
  "changelog": {
    "version": "2",
    "darkMode": true
  },
  "scripts": {
    "build": "npx @pinpt/changelog@VERSION build --site SITE",
    "dev": "npx @pinpt/changelog@VERSION dev --site SITE",
    "package": "npx @pinpt/changelog@VERSION package --site SITE"
  }
}
